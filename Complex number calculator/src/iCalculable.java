// интерфейс iCalculable
public interface iCalculable {

    // метод сложения
    iCalculable sum(int arg);

    //метод умножения
    iCalculable multi(int arg);

    // метод получения результата
    int getResult();
}
